import {
  extractDistrictCode,
  extractProvinceCode,
  extractRegencyCode,
} from '../../src/common/utils/code';

/**
 * Test helper utilities for extracting codes from area codes.
 * These functions use the utilities from the main source code.
 */

export { extractProvinceCode, extractRegencyCode, extractDistrictCode };
